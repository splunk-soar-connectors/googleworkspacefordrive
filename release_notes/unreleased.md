**Unreleased**

* Escaped API-controlled values before embedding them in the Drive user-list widget JavaScript context.
* Reported Drive lookup failures instead of treating every pre-delete error as a successful deletion.
